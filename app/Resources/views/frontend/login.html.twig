{% extends 'base.html.twig' %}
{% block title %}
Registro de usuario
{% endblock %}

{% block body %}

    <!-- Page Content -->
    <div class="container">
      <!-- Page Heading/Breadcrumbs -->
      <h1 class="mt-4 mb-3">BarberZone
        <small>Inicio de sesión</small>
        </h1>
     <!-- Intro Content -->
      <div class="row">
        <div class="col-lg-7">
            {% if error %}
                <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}
            
            <form action="{{ path('login') }}" method="post"> 
                
                <div class= "control-group form-group">
                    <div class="controls">
                        <label for="username">Correo electrónico:</label>
                        <input type="text" class= "form-control" id="username" name="_username" value="{{ last_username }}"  />
                    </div>
                </div>


                <div class= "control-group form-group">
                    <div class="controls">
                        <label for="password">Contraseña:</label>
                        <input type="password" class= "form-control" id="password" name="_password" />
                    </div>
                </div>
                
                <div>
                <input type="checkbox" id="remember_me" name="_remember_me" checked />
                <label for="remember_me">Mantenerme logueado</label>
                </div>

                <button type="submit" class="btn btn-primary">Iniciar sesión</button>


                <p>¿No estás registrado? <a href="{{ path('registro') }}">Regístrate ahora</a></p>
            </form>
        </div>
        <div class="col-lg-5">
          <h2>¿Por qué registrarme?</h2>
            <p>Al registrarte como usuario podrás realizar reservas a nuestra barbería de una manera <strong>fácil y sencilla</strong>.</p>
            <p>Mediante la introducción del correo electrónico y la contraseña, vas a poder realizar reservas o anulación de estas</p>
            <p>¡Ya no deberás de preocuparte por la disponibilidad de un turno nunca más! Nosotros te mostramos <strong>todas las fechas</strong> y <strong>horas disponibles</strong> para que puedas elegir el horario que más te guste.</p>
            <p>Sabemos que tu tiempo es importante y por eso, al registrarte, nuestra máxima prioridad es que tengas <strong>la mejor experiencia</strong> en la <strong>menor cantidad de tiempo</strong></p>
        </div>
      </div>        
      </h1>
    </div>

{% endblock %}
